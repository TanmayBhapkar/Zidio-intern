# ✅ Frontend Environment Variables

# Backend API URL (adjust if your backend runs on a different port or server)
REACT_APP_API_URL=http://localhost:5000/api

# React App Title (Optional - you can use it dynamically in your app)
REACT_APP_NAME=Blog App

# Default Environment
REACT_APP_ENV=development

# Debug Mode (Optional - turn off logs in production)
REACT_APP_DEBUG=true